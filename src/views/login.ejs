<!DOCTYPE html>
<html>
<head>
  <title>Login - MozaicHub</title>
  <link rel="stylesheet" href="/public/css/style.css" />
</head>
<body class="login-page">
  <div class="login-card">
    <h2>MozaicHub Login</h2>
    <% if (error) { %><div class="error"><%= error %></div><% } %>
    <% if (info) { %><div class="info"><%= info %></div><% } %>
    <form method="post" action="/login">
      <label>Username</label>
      <input type="text" name="username" required />
      <label>Password</label>
      <input type="password" name="password" required />
      <button type="submit">Login</button>
    </form>
    <div class="recover">
      <h4>Recover Account</h4>
      <form method="post" action="/recover">
        <input type="text" name="username" placeholder="Username" required />
        <button type="submit" class="ghost">Generate reset</button>
      </form>
      <p class="muted small">A randomized secure password will be logged to the server console.</p>
    </div>
  </div>
</body>
</html>
