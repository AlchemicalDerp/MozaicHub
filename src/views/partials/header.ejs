<!DOCTYPE html>
<html>
<head>
  <title>MozaicHub</title>
  <link rel="stylesheet" href="/public/css/style.css" />
</head>
<body class="theme-dark">
<nav class="topnav">
  <div class="logo"><a href="/">MozaicHub</a></div>
  <div class="links">
    <a href="/">Dashboard</a>
    <a href="/files">Browse</a>
    <a href="/files/mine">My Files</a>
    <a href="/friends">Friends</a>
    <a href="/messages">Messages</a>
    <% if (user && user.role === 'ADMIN') { %>
      <a href="/admin">Admin</a>
    <% } %>
    <button class="ghost" id="themeToggle" type="button">Toggle Theme</button>
    <a href="/notifications" class="notification-icon">
      <% if (unreadNotifications > 0) { %>
        <span class="badge"><%= unreadNotifications %></span>
      <% } %>
      🔔
    </a>
    <div class="avatar-link badge-anchor">
      <% if (unreadNotifications > 0) { %>
        <span class="badge-dot"></span>
      <% } %>
      <a href="/account" class="avatar-link">
        <% if (user && user.profileImagePath) { %>
          <img class="avatar-sm" src="/uploads/<%= user.profileImagePath %>" alt="Account" />
        <% } else { %>
          <div class="avatar-sm placeholder">You</div>
        <% } %>
      </a>
    </div>
  </div>
</nav>
<div class="container">
